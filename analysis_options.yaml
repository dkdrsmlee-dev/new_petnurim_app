# 이 파일은 Dart 정적 분석기 설정입니다.
# `flutter analyze` 명령으로 오류, 경고, 린트 규칙을 확인할 수 있습니다.

# Flutter 앱에 권장되는 기본 린트 규칙을 활성화합니다.
include: package:flutter_lints/flutter.yaml

linter:
  # 프로젝트에 필요한 린트 규칙은 아래에서 추가하거나 비활성화합니다.
  rules:
    # avoid_print: false
    # prefer_single_quotes: true

# 참고: https://dart.dev/guides/language/analysis-options
